// ┌─────────────────┐
// │ Your code here! │	
// └─────────────────┘

function ageToAbilities(number) {
    let num = getInput();
    for(let num of getInput <= 16) {
        return "You can't drive.";

    if(let num of getInput === 16 <= 17) 
        return "You can drive but not vote.";
    
    }else{ if (let num of getInput === 18 <= 24);
        return "You can vote, but not rent a car.";
    
    }else{ if (let num of getInput >= 25);
        return "You can do pretty much anything."; 
    }
    { return age;
}


function oddIndices(arr) {
    let odd = [];
    for (let i = 0; i < arr.length; i += 2) {
        if (odd[i] !== odd[0]) {
        arr.filter(arr[i]);
    }
    }
    return odd;
}

function numOddValues(arr) {
    for (const oddValues of numOddValues) {
        if (oddValues % 2 !== 0) {
            return oddValues;
        }
    }
}


function averageStringLength(arr) {
    let sumOfString = 0;
    for (let i = 0; i <arr.length; i++);
    sumOfString +=arr[i].length;
    
}


function firstPunctuationIndex(arr) {
    if arr[i]
}


function getPlace("highest", "lowest") {
    if highest 
}



// ┌─────────────────────────────────────┐
// │ Do not modify code below this line. │
// └─────────────────────────────────────┘

if (typeof ageToAbilities === 'undefined') {
    ageToAbilities = undefined;
}
if (typeof oddIndices === 'undefined') {
    oddIndices = undefined;
}
if (typeof numOddValues === 'undefined') {
    numOddValues = undefined;
}
if (typeof averageStringLength === 'undefined') {
    averageStringLength = undefined;
}
if (typeof firstPunctuationIndex === 'undefined') {
    firstPunctuationIndex = undefined;
}
if (typeof getPlace === 'undefined') {
    getPlace = undefined;
}

module.exports = {
    ageToAbilities,
    oddIndices,
    numOddValues,
    averageStringLength,
    firstPunctuationIndex,
    getPlace
};
